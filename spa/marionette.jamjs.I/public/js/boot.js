
require(["cs!app/app"], function(App) {
    var app = new App();
    app.initialize();
    return app;
    //var app = {version: '0.01'};
    //var app = App0;
    //console.log("App version is " + app.version + ".");
    });
